<%- include('../partials/header') %> 
<nav>
    <a href="/portfolios/" <%- title === 'Dashboard' ? 'class="active"' : '' %>>Dashboard</a>
    <a href="/portfolios/chart" <%- title === 'Chart' ? 'class="active"' : '' %>>Chart</a>
</nav>
    <h1><%= title %></h1>
    <p>Welcome to <%= title %></p>
    <a href="/logout">Logout</a>
    <% portfolios.forEach(function(portfolio) { %> 
        <a href="/portfolios/<%= portfolio.id%>">Portfolio <%= portfolio.portfolioID %></a>
    <%   }); %> 
    <form id="add-portfolio" actions="/portfolios" method="POST">
        <button>Add Portfolio</button>
    </form>
<%- include('../partials/footer') %> 